---
# distro-context job module configuration

# Where to write the context file (inside the target system)
context_file: "/etc/distro-bootloader/install-context.conf"

# Also write to a temp location for post-install scripts to read
temp_context_dir: "/tmp/distro-install-context"

# Whether to log the full context to the Calamares journal
log_context: true
