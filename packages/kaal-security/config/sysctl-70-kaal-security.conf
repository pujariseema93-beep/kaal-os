# =============================================================================
# 70-kaal-security.conf — KAAL OS kernel hardening profile
# =============================================================================
# Desktop-balanced hardening. Applied by script 18-security-hardening.sh.
# Rules that would break Flatpak, Distrobox, gamemode or debug workflows are
# deliberately NOT set (see README: user namespaces stay unrestricted,
# ptrace_scope=1 not 2).
# =============================================================================

# ---- Kernel information disclosure ----
# Hide kernel pointers in /proc and syslog (1 = non-root hidden)
kernel.kptr_restrict = 1

# Restrict dmesg to root (stops kernel address leaks to unprivileged users)
kernel.dmesg_restrict = 1

# ---- Userspace hardening ----
# ptrace: only parent may trace (gdb on own children still works)
kernel.yama.ptrace_scope = 1

# Disable unprivileged BPF (libbpf-based tools still work as root)
kernel.unprivileged_bpf_disabled = 1

# Harden the BPF JIT compiler (constant blinding)
net.core.bpf_jit_harden = 2

# ---- Filesystem protections ----
# (Fedora defaults, pinned here so a broken config cannot regress them)
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# ---- IPv4 ----
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.default.accept_source_route = 0
net.ipv4.icmp_echo_ignore_broadcasts = 1
net.ipv4.icmp_ignore_bogus_error_responses = 1
net.ipv4.tcp_syncookies = 1

# ---- IPv6 ----
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0
net.ipv6.conf.all.accept_source_route = 0
net.ipv6.conf.default.accept_source_route = 0
